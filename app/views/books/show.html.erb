<div class="container">
<div class="row">
<p id="notice">
 <%= notice %>
</p>
<div class="col-xs-3">
  <div class="profile-container">
    <h2 class="index_profile">User info</h2>
    <p><%= attachment_image_tag @user, :profile_image, format: 'jpeg', fallback: "download.jpg", size: "100x100"%></p>
    <table class="table table-striped">
      <tbody>
        <tr></tr>
        <tr>
          <th>name</th>
          <th><%= current_user.name %></th>
        </tr>
        <tr>
          <th>introduction</th>
          <th><%= current_user.introduction %></th>
        </tr>
      </tbody>
    </table>
    <%=link_to"" ,edit_user_path(current_user.id),class:"col-xs-12 btn btn-default glyphicon glyphicon-wrench edit_user_20224" %>
  </div>
    <h2>New book</h2>
    <% if @book.errors.any? %>
<p>2errors prohibited this book from being saved:</p>
 <ul>
   <li>Title can't be blank</li>
   <li>Body can't be blank</li>
 </ul>
 <% end %>
 <%= form_for(@book) do |f| %>
   <h4>Title</h4>
   <%= f.text_field :title,class:"col-xs-12" %>
   <h4>Opinion</h4>
   <%= f.text_area :body ,class:"col-xs-12"%>
   <%= f.submit 'Create Book', class:"btn btn-primary col-xs-12" %>
 <% end %>
</div>
<div class="col-xs-9">
  <table class="table table-striped">
    <h2>Book details</h2>
    <tbody>
      <tr>
      <td>
        <%= link_to user_path(@book.user.id) do %>
    <%= attachment_image_tag @user, :profile_image, format: 'jpeg', fallback: "download.jpg", size: "100x100"%>
    <%= @book.user.name %>
    <% end %>
    </td>
<td><%= @book.title %></td>
<td><%= @book.body %></td>
<% if @book.user_id == current_user.id %>
<td><%= link_to "Edit", edit_book_path(@book.id),class:"btn-sm btn-success" %></td>
<td><%=link_to "Destory", book_path(@book.id), method: :delete, class:"btn-sm btn-denger" %></td>
<% end %>
</tr>
</tbody>
</table>
</div>
</div>
</div>
