<h1 class="edit_page">User info</h1>
<% if @user.errors.any? %>
<p>2errors prohibited this book from being saved:</p>
   <ul>
     <li>Title can't be blank</li>
     <li>Body can't be blank</li>
   </ul>
   <% end %>
<%= form_for(@user) do |f| %>
<p><%= f.text_field :name, autofocus: true, class:"form-control", id:"inputEmail", placeholder:"名前"%></p>
<p><%= f.attachment_field :profile_image, placeholder: "プロフィール画像", class: "user-edit-form" %></p>
<p><%= f.text_area:introduction %></p>
<p><%= f.submit 'Update User' %></p>
<%end%>
